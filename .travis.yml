language: python
dist: trusty
os: linux
python:
  - "3.6"

install:
  - cd tf-yarn
  - pip install .
  - pip install pytest
  - pip freeze

script:
  - pwd
  - pytest tf-yarn/tests -vv

notifications:
  email: false
